name: katrix_biometrics
description: "Katrix Biometrics – autenticación biométrica multi-plataforma para apps Katrix. Soporta local_auth (Android/iOS/Desktop) y WebAuthn (PWA/Web)."
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  local_auth: ^2.3.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
