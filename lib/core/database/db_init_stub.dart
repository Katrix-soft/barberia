import 'package:sqflite/sqflite.dart';

Future<void> initializeDatabaseFactory() async {
  // Desktop/Mobile specific init
  try {
    // We import dynamically if possible or just handle it in the IO file
  } catch (e) {}
}
