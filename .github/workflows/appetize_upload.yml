name: Upload to Appetize

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      # 📦 Checkout del repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🐦 Instalar Flutter
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      # 🔍 Ver estado del entorno
      - name: Flutter doctor
        run: flutter doctor -v

      # 📦 Dependencias Flutter
      - name: Install dependencies
        run: flutter pub get

      # 🍎 Instalar CocoaPods (clave en CI)
      - name: Install CocoaPods
        run: |
          sudo gem install cocoapods
          pod repo update

      # ⚙️ Setup iOS (pods)
      - name: Setup iOS
        run: |
          cd ios
          pod install
          cd ..

      # 🔨 Build para simulador (con logs completos)
      - name: Build iOS Simulator
        run: |
          flutter build ios --simulator --no-codesign -v
          cd build/ios/iphonesimulator
          zip -r app.zip Runner.app

      # 🚀 Subir a Appetize
      - name: Upload to Appetize
        run: |
          curl -X POST https://api.appetize.io/v1/apps \
            -H "X-API-KEY: ${{ secrets.APPETIZE_API_TOKEN }}" \
            -F "file=@build/ios/iphonesimulator/app.zip" \
            -F "platform=ios"
