name: barber_backend
description: Backend server for BM Barber - handles Mercado Pago webhooks and other API logic.
version: 1.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  http: ^1.1.0
  sqflite_common_ffi: ^2.3.0
  dotenv: ^4.2.0
  crypto: ^3.0.3

dev_dependencies:
  lints: ^3.0.0
